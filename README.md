# rain-alert-notifier
A Python-powered automated weather monitor that tracks upcoming 12-hour forecasts via the OpenWeatherMap API and dispatches a Twilio SMS text alert if rain is expected. Built securely using environment variables to protect API keys.
